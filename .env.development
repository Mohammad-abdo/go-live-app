# Dev proxy target for `/apimobile` and `/api` (see vite.config.js).
# Use your deployed API host (no trailing slash), or http://127.0.0.1:5000 for local backend.
VITE_API_PROXY_TARGET=https://qeema-track.developteam.site
